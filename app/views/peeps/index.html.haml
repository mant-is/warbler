%section.section
  .container
    .columns
      - if user_signed_in?
        = render 'profile'
      - else
        = render 'trends'
      = render 'feed'
      = render 'who-to-follow'
      = render 'devise/shared/links'
