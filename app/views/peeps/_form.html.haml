= form_with(model: peep) do |form|
  - if peep.errors.any?
    #error_explanation
      %h2
        = pluralize(peep.errors.count, "error")
        prohibited this peep from being saved:
      %ul
        - peep.errors.each do |error|
          %li= error.full_message
  .field
    = form.label :chirp
    = form.text_area :chirp
  .actions
    = form.submit
